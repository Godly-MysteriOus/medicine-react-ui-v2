import './App.css'
import { createTheme } from '@mui/material/styles';

function App() {
  
  const defaultTheme = createTheme();
  console.log('Full MUI Theme Object:', defaultTheme);
  return (<></>)
}

export default App
